<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ham.mapper.MyUserProfileMapper">
	
	<select id="edit" resultType="com.ham.domain.MyMemberDTO">
		select
			m_id,
			m_pw,
			m_name,
			m_address,
			m_address_detail,
			m_tel,
			m_email,
			m_birth
		from tblMember
			where m_id=#{id}
	</select>
	
	<update id="update">
		update tblMember set
			m_email = #{m_email},
			m_address = #{m_address},
			m_address_detail = #{m_address_detail},
			m_tel = #{m_tel}
				where m_id = #{m_id}
	</update>
	
	<update id="updatepw">
		update tblMember set
			m_pw = #{m_pw}
				where m_id = #{m_id}
	</update>

</mapper>